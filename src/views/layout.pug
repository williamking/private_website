doctype html
html(lang="zh-CN")
    head
        include ./include/head
    body
        header(id="header")
        canvas.rain(id="rain")
        div.mask

        div#wallpaper-slider

        div#main-wrapper
            block content

        footer(id="footer")

        include ./include/scripts

        block script
